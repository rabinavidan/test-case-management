pytest==7.4.3
pytest-cov==7.1.0
# Parallelizes tests/unit tests/api tests/contract tests/services across
# worker processes (-n auto in test.yml). Safe because every DB-backed
# fixture (tests/api/conftest.py, tests/contract/test_openapi_contract.py,
# tests/services/conftest.py) derives a worker-unique SQLite path/tempdir —
# see their comments — so parallel workers never race the same file.
pytest-xdist==3.8.0
# Bumped from 0.25.2: the `ollama` package (a langchain-ollama dependency,
# see below) requires httpx>=0.27. Pinned at 0.27.2 rather than latest
# (0.28+) because httpx 0.28 removed the `Client(app=...)` shortcut
# starlette's TestClient (used throughout tests/api, tests/contract) relies
# on — 0.27.x is the newest line that still supports it.
httpx==0.27.2
playwright==1.46.0
pytest-playwright==0.5.0
pytest-json-report==1.5.0
allure-pytest==2.13.5
# Pinned to the 14.x line: 15.x+ requires pytest>=8.2, which conflicts with
# the pytest==7.4.3 pin above.
pytest-rerunfailures==14.0
# Pinned to the 3.x line: 4.x requires pytest>=9, which conflicts with the
# pytest==7.4.3 / pytest-playwright pins above.
schemathesis==3.39.16
# Only used by tests/services/ (the microservices layer's runs service imports
# it at module load, whether or not a Redis is actually reachable in CI).
redis==5.2.1
# Only used by tests/infra/ to parse `kubectl kustomize` output.
PyYAML==6.0.1
# agents/test_plan_reviewer.py (LangChain-based) and its unit tests — the
# latter use langchain_core's own FakeListChatModel, never a real Ollama
# server. langchain-ollama is only imported lazily inside
# build_ollama_chat_model(), but still needs to be installed for the tests
# that call it (construction doesn't make a network call).
langchain-core==1.6.3
langchain-ollama==1.1.0
