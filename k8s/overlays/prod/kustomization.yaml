apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: testflow-prod

resources:
  - ../../base
  - namespace.yaml
  - ingress.yaml

patches:
  - target:
      kind: Deployment
    patch: |-
      - op: add
        path: /spec/template/spec/nodeSelector
        value:
          kubernetes.io/hostname: node-prod-1

replicas:
  - name: gateway
    count: 3
  - name: auth
    count: 2
  - name: projects
    count: 2
  - name: runs
    count: 3
  - name: ai
    count: 2

configMapGenerator:
  - name: testflow-config
    behavior: merge
    literals:
      - ENVIRONMENT=prod

secretGenerator:
  - name: testflow-secrets
    behavior: merge
    literals:
      - JWT_SECRET_KEY=REPLACE_ME_prod_jwt_secret

# Prod pins the exact version that passed preprod — never `:latest`.
images:
  - name: testflow/gateway
    newTag: v2.0.66
  - name: testflow/auth
    newTag: v2.0.66
  - name: testflow/projects
    newTag: v2.0.66
  - name: testflow/runs
    newTag: v2.0.66
  - name: testflow/ai
    newTag: v2.0.66
