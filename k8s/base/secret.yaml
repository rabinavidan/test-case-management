# Placeholder values only — every overlay replaces these via `secretGenerator`
# (see overlays/<env>/kustomization.yaml). In a real cluster, source them from
# a secret manager (Sealed Secrets / External Secrets Operator / Vault) instead
# of committing literals, staging included.
apiVersion: v1
kind: Secret
metadata:
  name: testflow-secrets
type: Opaque
stringData:
  POSTGRES_PASSWORD: "changeme"
  DATABASE_URL: "postgresql://testflow:changeme@db:5432/testflow"
  JWT_SECRET_KEY: "changeme"
  ANTHROPIC_API_KEY: ""
