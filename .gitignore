__pycache__/
*.py[cod]
*.db
.env
.venv/
venv/
*.egg-info/
dist/
build/
.vercel/
.idea/
allure-results/
allure-report/
test-results/
.coverage
coverage.json
coverage.xml
htmlcov/

# Terraform (terraform/) — local provider cache/plugins and any real
# tfvars/plan output; .terraform.lock.hcl IS committed (pins provider versions).
**/.terraform/
*.tfstate
*.tfstate.*
*.tfplan
*.auto.tfvars
terraform.tfvars

# Claude Code guidance files — kept locally, not pushed to PRs/main.
# Exception: .claude/skills/**/*.md — shared skill files (e.g. steward/
# SKILL.md) that a CI-triggered Claude Code session needs to read, so
# those *are* committed.
/*CLAUDE*.md
.claude/**/*.md
!.claude/skills/**/*.md
